{
  "name": "fearless",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "deploy": "bun scripts/deploy-commands.ts",
    "deploy-test": "bun scripts/deploy-commands-test.ts",
    "reset": "bun scripts/reset-commands.ts",
    "reset-test": "bun scripts/reset-commands-test.ts",
    "start": "bun .",
    "dev": "APP_ENV=test bun ."
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "discord.js": "^14.25.1",
    "path": "^0.12.7"
  }
}
